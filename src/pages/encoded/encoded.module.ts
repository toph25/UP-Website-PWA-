import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { EncodedPage } from './encoded';

@NgModule({
  declarations: [
    //EncodedPage,
  ],
  imports: [
    //IonicPageModule.forChild(EncodedPage),
  ],
})
export class EncodedPageModule {}
